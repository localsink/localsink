{
  "name": "localsink",
  "version": "0.0.0",
  "description": "Local-first log sink with a searchable UI and API (reserved).",
  "license": "BUSL-1.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/localsink/localsink"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsgo -b .",
    "lint": "oxlint .",
    "test": "vitest run",
    "dev": "node --watch --watch-path=src src/server.ts",
    "start": "node src/server.ts",
    "drizzle-kit:push": "drizzle-kit push",
    "drizzle-kit:generate": "drizzle-kit generate",
    "drizzle-kit:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@hono/mcp": "catalog:",
    "@hono/node-server": "catalog:",
    "@hono/zod-validator": "catalog:",
    "@libsql/client": "catalog:",
    "@modelcontextprotocol/sdk": "catalog:",
    "drizzle-orm": "catalog:",
    "drizzle-zod": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  }
}
